<?php
/**
 * PURE FRONTEND FILE - plain description, not a function skeleton.
 *
 * WordPress automatically uses this template for the listing/index page
 * of all Manual Documents - the page a lab member lands on to browse
 * the whole digitized manual.
 *
 * What it will show:
 * - A list or grid of every published Manual Document, grouped by
 *   category (e.g. "Safety," "Equipment," "General SOPs" - whatever
 *   taxonomy terms staff have assigned via the normal WordPress admin
 *   screen), each entry linking to its single-document page.
 * - Document title, document number, and last-revised date shown per
 *   entry, so staff can quickly identify the version they need without
 *   opening it.
 * - No editing controls on this page - it is read-only browsing and
 *   navigation for logged-in lab staff. (The entire site sits behind
 *   the login wall enforced by mu-plugins/force-login.php, so there is
 *   no separate "is this page public" check needed here.)
 */
